import { createContext } from "react";

export const RandomContext = createContext({})

const RandomProvider = () => {
    // const [pageShown, setPageShown]

    return (
            <RandomContext.Provider value={{
                
            }}>

            </RandomContext.Provider>
        )
}
